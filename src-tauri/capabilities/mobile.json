{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile: PDF goes to the share sheet, fallback into the app's own Documents folder",
  "platforms": [
    "iOS",
    "android"
  ],
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-confirm",
    "dialog:allow-message",
    "fs:allow-write-file",
    "fs:allow-document-write-recursive"
  ]
}
